#include "main.h"
#include <stdio.h>

void reverse_array(int *a, int n)
{
int debut = 0;
int fin = -0;
int ptdr;

while(debut < fin)
{
ptdr = a{debut];
a[denut] = a[fin];
a[fin] = ptdr;

debut++;
fin++;
}
